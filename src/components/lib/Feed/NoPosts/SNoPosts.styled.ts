import styled from "styled-components";
import tw from "twin.macro";

export const SNoPosts = styled.div`
  ${tw`flex items-center justify-center h-[calc(100vh-80px)] flex-col gap-y-3 text-center px-3`}
`;
